# Copy from GCR multi-region to new AR region.
- name: "gcr.io/go-containerregistry/gcrane"
  args: ["cp", "-j", "100", "-r", "${_SRC_GCR_REPO}", "${_DEST_AR_REPO}"]
  # id: "europe"
  waitFor: ["-"]
